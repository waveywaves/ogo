name: Release

on:
  push:
    tags:
      - 'v*'

env:
  IMAGE_BASE: quay.io/aknochow
  GHCR_BASE: ghcr.io/aknochow

jobs:
  gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9
        with:
          version: v2.12.2

      - name: Test
        run: make test

  build:
    needs: gate
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: ogo
            containerfile: Containerfile
            runner: ubuntu-latest
            arch: amd64
          - image: ogo
            containerfile: Containerfile
            runner: ubuntu-24.04-arm
            arch: arm64
          - image: ogo-auth-bridge
            containerfile: Containerfile.auth-bridge
            runner: ubuntu-latest
            arch: amd64
          - image: ogo-auth-bridge
            containerfile: Containerfile.auth-bridge
            runner: ubuntu-24.04-arm
            arch: arm64

    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Login to Quay.io
        run: podman login -u "$QUAY_USERNAME" -p "$QUAY_PASSWORD" quay.io
        env:
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}

      - name: Build image
        run: podman build -f ${{ matrix.containerfile }} -t "${IMAGE_BASE}/${{ matrix.image }}:${TAG}-${{ matrix.arch }}" .
        env:
          TAG: ${{ github.ref_name }}

      - name: Push image
        run: podman push "${IMAGE_BASE}/${{ matrix.image }}:${TAG}-${{ matrix.arch }}"
        env:
          TAG: ${{ github.ref_name }}

  manifest:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        image: [ogo, ogo-auth-bridge]

    steps:
      - name: Login to Quay.io
        run: podman login -u "$QUAY_USERNAME" -p "$QUAY_PASSWORD" quay.io
        env:
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}

      - name: Create and push manifest
        run: |
          podman manifest create "${IMAGE_BASE}/${IMAGE}:${TAG}"
          podman manifest add "${IMAGE_BASE}/${IMAGE}:${TAG}" "docker://${IMAGE_BASE}/${IMAGE}:${TAG}-amd64"
          podman manifest add "${IMAGE_BASE}/${IMAGE}:${TAG}" "docker://${IMAGE_BASE}/${IMAGE}:${TAG}-arm64"
          podman manifest push "${IMAGE_BASE}/${IMAGE}:${TAG}" "docker://${IMAGE_BASE}/${IMAGE}:${TAG}"
        env:
          IMAGE: ${{ matrix.image }}
          TAG: ${{ github.ref_name }}

      - name: Tag as latest (stable releases only)
        if: ${{ !contains(github.ref_name, '-') }}
        run: |
          skopeo copy --all \
            "docker://${IMAGE_BASE}/${IMAGE}:${TAG}" \
            "docker://${IMAGE_BASE}/${IMAGE}:latest"
        env:
          IMAGE: ${{ matrix.image }}
          TAG: ${{ github.ref_name }}

      - name: Inspect manifest
        run: skopeo inspect --raw "docker://${IMAGE_BASE}/${IMAGE}:${TAG}" | python3 -m json.tool
        env:
          IMAGE: ${{ matrix.image }}
          TAG: ${{ github.ref_name }}

  mirror:
    needs: manifest
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        image: [ogo, ogo-auth-bridge]

    steps:
      - name: Login to Quay.io
        run: podman login -u "$QUAY_USERNAME" -p "$QUAY_PASSWORD" quay.io
        env:
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}

      - name: Login to GHCR
        run: podman login -u "$GHCR_USER" -p "$GHCR_TOKEN" ghcr.io
        env:
          GHCR_USER: ${{ github.repository_owner }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Mirror to GHCR
        run: |
          skopeo copy --all \
            "docker://${IMAGE_BASE}/${IMAGE}:${TAG}" \
            "docker://${GHCR_BASE}/${IMAGE}:${TAG}"
        env:
          IMAGE: ${{ matrix.image }}
          TAG: ${{ github.ref_name }}

      - name: Mirror latest to GHCR (stable releases only)
        if: ${{ !contains(github.ref_name, '-') }}
        run: |
          skopeo copy --all \
            "docker://${IMAGE_BASE}/${IMAGE}:latest" \
            "docker://${GHCR_BASE}/${IMAGE}:latest"
        env:
          IMAGE: ${{ matrix.image }}

  bundle:
    needs: manifest
    if: ${{ !contains(github.ref_name, '-') }}
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Login to Quay.io
        run: podman login -u "$QUAY_USERNAME" -p "$QUAY_PASSWORD" quay.io
        env:
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_PASSWORD: ${{ secrets.QUAY_PASSWORD }}

      - name: Set version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate bundle
        run: make bundle IMG="${IMAGE_BASE}/ogo:${TAG}" VERSION="${VERSION}"
        env:
          TAG: ${{ github.ref_name }}
          VERSION: ${{ steps.version.outputs.version }}

      - name: Build and push bundle image
        run: |
          podman build -f bundle.Dockerfile -t "${IMAGE_BASE}/ogo-bundle:${TAG}" .
          podman push "${IMAGE_BASE}/ogo-bundle:${TAG}"
          podman tag "${IMAGE_BASE}/ogo-bundle:${TAG}" "${IMAGE_BASE}/ogo-bundle:latest"
          podman push "${IMAGE_BASE}/ogo-bundle:latest"
        env:
          TAG: ${{ github.ref_name }}

      - name: Build and push catalog image
        run: |
          podman build -f catalog.Dockerfile -t "${IMAGE_BASE}/ogo-catalog:${TAG}" .
          podman push "${IMAGE_BASE}/ogo-catalog:${TAG}"
          podman tag "${IMAGE_BASE}/ogo-catalog:${TAG}" "${IMAGE_BASE}/ogo-catalog:latest"
          podman push "${IMAGE_BASE}/ogo-catalog:latest"
        env:
          TAG: ${{ github.ref_name }}

  release:
    needs: [manifest, bundle]
    if: >-
      always() &&
      needs.manifest.result == 'success' &&
      (needs.bundle.result == 'success' || needs.bundle.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Generate changelog
        id: changelog
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5 # v4
        with:
          config: cliff.toml
          args: --verbose --latest --strip header

      - name: Collect dependency versions
        id: deps
        run: |
          OPENSHELL=$(grep -oP 'OpenShell-v\K[\d.]+' docs.toml | head -1)
          GO_VERSION=$(go version | grep -oP 'go\K[\d.]+')
          CONTROLLER_RUNTIME=$(go list -m -f '{{ .Version }}' sigs.k8s.io/controller-runtime)
          K8S_API=$(go list -m -f '{{ .Version }}' k8s.io/api)
          OPERATOR_SDK=$(grep -oP 'OPERATOR_SDK_VERSION \?= v\K[\d.]+' Makefile)

          cat > /tmp/deps.md <<DEPSEOF

          ---

          ### Dependencies

          | Component | Version |
          |-----------|---------|
          | OpenShell Gateway | v${OPENSHELL} |
          | Go | ${GO_VERSION} |
          | controller-runtime | ${CONTROLLER_RUNTIME} |
          | k8s.io/api | ${K8S_API} |
          | operator-sdk | v${OPERATOR_SDK} |
          DEPSEOF

          echo "deps<<GHEOF" >> "$GITHUB_OUTPUT"
          cat /tmp/deps.md >> "$GITHUB_OUTPUT"
          echo "GHEOF" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          body: |
            ${{ steps.changelog.outputs.content }}
            ${{ steps.deps.outputs.deps }}
          prerelease: ${{ contains(github.ref_name, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

